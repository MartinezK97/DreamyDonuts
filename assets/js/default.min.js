$(document).ready(function () {
    let e = document.querySelector("#content-all"),
        o = document.querySelector(".scrollH"),
        l = document.querySelectorAll(".product"),
        t = document.querySelectorAll(".product img");
    if (e && o && l.length > 0) {
        let r = l.length, c = l[0].clientHeight,
            n = document.querySelector(".product-content").clientWidth;
        console.log("Altura de cada producto:", c),
            console.log("Ancho de cada producto:", n);
        let a = e.scrollHeight - e.clientHeight,
            d = o.scrollWidth - o.clientWidth;
        console.log("M\xe1ximo scroll vertical:", a),
            console.log("M\xe1ximo scroll horizontal:", d),
            console.log("Cantidad de productos:", r),
            e.addEventListener("scroll", function () {
                var l = e.scrollTop / a * 100; console.log("Scroll vertical en %", l),
                    calcScrollH = l * d / 100, o.scrollLeft = calcScrollH; let r = calcScrollH / (2 * Math.PI * (n / 3)) * 360;
                console.log("Grados de rotacion: ", r),
                    t.forEach(e => {
                        e.style.transform = `rotateZ(-${r}deg)`
                    })
            })
    } else console.error("Error: No se encontraron los elementos necesarios o no tienen scroll.")
}), document.addEventListener("DOMContentLoaded", function () {
    let e = document.querySelector("#portada video"); e && (e.playbackRate = .5,
        setInterval(() => { .75 !== e.playbackRate && (e.playbackRate = .75) }, 500))
}); 